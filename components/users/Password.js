import styled from "styled-components";

const Section = styled.section`
  width: 100%;
  text-align: center;
`;

export default function Password() {
  return (
    <Section>
      <p>비밀번호 찾기 페이지는 준비 중 입니다.</p>
    </Section>
  );
}
